// Aqui definimos el estado y las funciones que modifican el estado
// Por lo gral definimos un estado dentro de un OBJETO

//Arreglo de usuarios users = [] vacio - contiene listado de usuarios completo
//Objeto selectedUser:{} vacio - contiene el profile del usuario seleccionado

const initialState = {
  users: [],
  selectedUser: {},
};

import React from "react";

const UserContext = () => {
  return <div></div>;
};

export default UserContext;
